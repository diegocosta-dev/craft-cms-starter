{#

Picture Block component

Usage:

{{ include('_components/_contentBlocks/_picture', { Parameters }) }}

Parameters:

- blk: The block object

Picture Block object model:

{% set blk = {
  image: Asset,
} %}

#}
{# prettier-ignore-start #}
{% if blk is defined and blk and blk.image is defined and blk.image %}

  {{ include('_components/_img', {
    imgAsset: blk.image.one(),
    classes: 'w-full'
  }) }}

{% endif %}